{
  "name": "hive",
  "version": "0.5.0",
  "private": false,
  "type": "module",
  "bin": {
    "hive": "./bin/hive.mjs"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "hive": "node ./bin/hive.mjs"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
